services:
  - type: web
    name: portfolio-api
    runtime: node
    rootDir: portfolio-api
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
